// Importing the AboutContent component from the specified path
import AboutContent from '@/components/about/about-content';

// Defining the AboutPage component as the default export
export default function AboutPage() {
  // Rendering the AboutContent component within the AboutPage
  return <AboutContent />;
}